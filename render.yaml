services:
  - type: web
    name: spa-ripper-web
    runtime: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /api/health
    envVars:
      - key: SPA_RIPPER_ALLOW_PRIVATE
        value: "0"
